mbSniffer can be easily built using Qt5 qmake:

mkdir build
cd build
qmake ../mbSniffer
make

and that's it. Alternatively open the project with Qt Creator and build
it with the according tool buttons.

